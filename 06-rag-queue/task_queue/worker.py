async def process_query(query):
    # Simulate processing the query (e.g., calling an LLM, retrieving documents, etc.)
    import time
    time.sleep(5)  # Simulating a time-consuming task
    return f"Processed result for query: {query}"